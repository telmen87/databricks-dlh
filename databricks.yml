# This is a Databricks asset bundle definition for db_dlh.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: db_dlh
  uuid: d980e297-c720-4b0e-98f1-fe274339614a

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-f5e9344f-c955.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-f5e9344f-c955.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/telmen87@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/telmen87@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: telmen87@gmail.com
        level: CAN_MANAGE
